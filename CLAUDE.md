# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Project

`images-library` is a Python utility that organizes images into a date-based directory tree: `YYYY/MM/YYYY-MM-DD/`.

## Status

The project is in early setup — no source code, packaging config, or test suite exists yet. The `.gitignore` is pre-configured for Python (including common virtual env, build, and test artifacts).

## Setup

No toolchain has been chosen yet. The `.gitignore` includes stubs for Poetry, PDM, Pixi, and pip/venv workflows. When a package manager is adopted, add its run commands here.

## License

GNU Affero General Public License v3 (AGPL-3.0). Any changes must be shared under the same terms.
